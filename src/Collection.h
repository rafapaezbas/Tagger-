#include "FileManager.h"
#include "Tagger.h"
#include <vector>
#include <string>
#include <algorithm>

struct Artist;
struct Album;
void createCollectionFromDir(std::string path,std::string outputPath);
